.pio/
.vscode/
node_modules/
dist/
build/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
# ...but DO track the example/template file itself — it's not a secret, and
# without it nobody knows VITE_API_BASE_URL exists or how to set it for LAN
# access. ".env.*" above was silently deleting this from every commit.
!.env.example
!**/.env.example
*.log
.DS_Store
Thumbs.db
*.tmp
*.temp